#!/bin/bash
if [ -z "$1" ]; then
    echo "Usage: eman <page>"
    exit 1
fi
if ! man -w "$1" > /dev/null 2>&1; then
    echo "No manual entry for $1"
    exit 1
fi
exec emacsclient -nw -c -e "(progn (setq Man-notify-method 'pushy) (man \"$1\"))"
