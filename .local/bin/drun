#!/bin/bash
if pgrep -x dmenu > /dev/null; then
    # If dmenu is open, focus it
    i3-msg '[class="(?i)dmenu"] focus'
else
    # Launch dmenu_run and exit this script immediately
    dmenu_run "$@"
fi
