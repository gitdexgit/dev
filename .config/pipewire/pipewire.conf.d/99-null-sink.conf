context.objects = [
    { factory = adapter
        args = {
            factory.name     = support.null-audio-sink
            node.name        = "PhoneStream"
            node.description = "PhoneStream"
            media.class      = Audio/Sink
            audio.position   = [ FL FR ]
        }
    }
]
